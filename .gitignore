.next/
node_modules/
.env.local
.env.*.local
*.log


.vercel

# Claude Code local worktrees/state
.claude/

# ss-ingest bridge (server-only secrets + log)
scripts/ss-ingest-bridge.env.cmd
scripts/ss-ingest-bridge.log

# TypeScript incremental build state — machine-local, never commit
tsconfig.tsbuildinfo
*.tsbuildinfo
