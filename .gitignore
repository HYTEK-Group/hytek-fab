.next/
node_modules/
.env.local
.env.*.local
*.log


.vercel

# Claude Code local worktrees/state
.claude/

# ss-ingest bridge (server-only secrets + log)
scripts/ss-ingest-bridge.env.cmd
scripts/ss-ingest-bridge.log

# Build artefact — regenerated by `next build` / `tsc`; tracking it dirties
# every working tree that builds (Lane 0 note, 07/09/2026).
tsconfig.tsbuildinfo
